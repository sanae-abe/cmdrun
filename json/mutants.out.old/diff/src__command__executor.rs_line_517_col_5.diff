--- src/command/executor.rs
+++ replace detect_shell -> String with String::new()
@@ -509,32 +509,17 @@
             }
         }
         "cdx".to_string() // デフォルト
     }
 }
 
 /// デフォルトシェル検出
 pub fn detect_shell() -> String {
-    if cfg!(windows) {
-        // Windows: PowerShell Core → PowerShell → cmd の優先順位
-        if which::which("pwsh").is_ok() {
-            "pwsh".to_string()
-        } else if which::which("powershell").is_ok() {
-            "powershell".to_string()
-        } else {
-            "cmd".to_string()
-        }
-    } else {
-        // Unix系: SHELL環境変数 → bash
-        std::env::var("SHELL")
-            .ok()
-            .and_then(|s| s.split('/').next_back().map(String::from))
-            .unwrap_or_else(|| "bash".to_string())
-    }
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::config::CommandSpec;
 
     #[test]
